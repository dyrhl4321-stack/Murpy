{
  "rules": {
    ".read": false,
    ".write": false,
    "squadRooms": {
      "$sid": {
        "players": {
          ".read": "auth != null",
          "$uid": {
            ".write": "auth != null && auth.uid == $uid"
          }
        }
      }
    }
  }
}
