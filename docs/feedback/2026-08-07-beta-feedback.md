# 클로즈 베타 유저 피드백 (2026-08-07 접수)

근방단 배포 후 들어온 22건. **우선순위는 "지금 들어온 사람이 막히는가"로 잡는다** —
콜드스타터가 첫날 겪는 것부터 고친다. CC 논리상 초반 Churn 이 제일 비싸다.

상태: ⬜ 미착수 / 🔧 진행 / ✅ 완료

---

## A. 지금 막고 있는 것 (최우선)

| # | 내용 | 상태 | 메모 |
|---|---|---|---|
| 2 | **카톡 링크로 바로 로그인 안 됨** | ✅ | 원인=인앱브라우저 안에서 `throughTalk:true` 로 카톡을 또 엶. 인앱이면 끄고 + 도움시트(기본 브라우저로 열기·홈 화면 추가법). `3c4e99a`→`abd9179` |
| 17 | 베타 설문에서 인앱 뒤로가기 안 됨 | ✅ | `/survey` 헤더에 돌아가기 버튼 (`3c4e99a`) |
| 21 | 받은 좋아요 — 갯수/색이 홈 나갔다 와야 반영. 상대 프로필 탭 무반응 | ✅ | 별개 버그 2개였다. ①하트 갱신이 패널 열 때·매칭 탭 진입 때만 → `likes` 실시간 구독 ②`.panel` 이 전부 z-index 100 이라 **DOM 순서**로 겹침 결정 → 관심 패널이 프로필 패널을 덮고 있었다. openPanel 이 깊이만큼 z를 올린다 |
| 6 | 닉네임 수정했는데 이전 닉네임이 남음 | ✅ | 원인 2개. ①`_mwMyNick` 이 앱 열 때 1회만 채워져 그 세션 내내 옛 이름(머피월드 라벨·스쿼드 메시지) ②**닉네임은 글마다 복사돼 저장된다(비정규화)** → 이미 쓴 글엔 옛 이름이 남는다. 저장 시 `_propagateNickname()` 이 feed·bamboo·스쿼드 멤버를 훑어 갱신. + 센터 리뷰가 `displayName`(계정 실명)을 박던 것도 수정 |
| 16 | 캐릭터 저장 버튼이 꾸미기 안에 있어 저장된 줄 모름 | ✅ | 대표 선택=**자동 저장**. 버튼 제거하고 고르는 즉시 저장(0.6초 debounce), 그 자리에 상태 표시(고르면 자동 저장돼요 / 저장 중 / 저장됨 / 실패). 착용·벗기·몸통교체 전부 훅 |
| 5 | 머피캠 필터에서 캐릭터가 빡빡이(base)로만 나옴 | ✅ | base 가 빡빡이라 **헤어 슬롯이 비면 민머리가 드러난다.** 비는 경로 2개 — ①성별 몸통 백필이 남캐 헤어를 벗기고 **여캐 기본헤어를 안 채움** ②카탈로그에서 내린 헤어를 끼고 있던 유저(`_HAIR_ID_MIGRATE` 미등록). `_charEnsureDefaults()` 로 한 곳에서 메움(로드 시 1회 저장 + 사진 합성 직전 한 번 더). 머피캠뿐 아니라 오버월드·프로필도 같이 나음 |
| 3 | 사진 크롭 화면에 전체 스크롤이 없음 | ✅ | `#crop-modal` 이 `inset:0`+flex 만 있고 스크롤이 없어 넘친 부분이 잘렸다. ①모달에 세로 스크롤 ②헤더(취소·다음) `position:sticky` ③크롭영역 `100dvh-150px`→`-190px`(헤더+안내칩+안전영역을 실제로 빼야 한 화면에 든다) |
| 7 | PC/노트북에서 우측 뒤로가기 UI 등이 남음 | ⬜ | 데스크톱에서만 숨기기 |

### #5 후속 — 이미 올라간 피드 사진 (대표 지시 2026-08-07)
사진은 캐릭터를 **구워서** imgBB 에 올린다(원본 미보관) → 코드를 고쳐도 지난 사진은 안 낫는다.
캐릭터 좌표가 고정 상수라 **같은 자리에 다시 그려 덮는** 관리자 도구를 만들었다:
마이페이지 하단 **"지난 피드 빡빡이 고치기 (관리자)"**. 전/후를 나란히 보여주고
**대표가 누른 것만** 교체한다(자동 적용 안 함 — 남의 게시물이고 되돌릴 수 없다).
★주의: 올린 뒤 옷을 바꾼 사람은 그 사진의 옷도 지금 것으로 바뀐다.

## B. 데이터 (대표 액션 필요)

| # | 내용 | 상태 | 메모 |
|---|---|---|---|
| 1 | 짐박스 일원점 추가 | ✅ | 대표 파일의 4곳(일원역점·사당1·짐맥스·사당2) 반영. **관리자 정비 버튼**으로 등록 |
| 9 | 짐박스 송파점 (오픈예정) → 오픈 완료로 수정 | ✅ | 관리자 정비 버튼의 `_CENTER_RENAME` 에서 이름 교체 |

## C. 기능 개선

| # | 내용 | 상태 | 메모 |
|---|---|---|---|
| 4 | 대숲 본인 글 삭제 | ✅ | 짐작대로 규칙은 처음부터 열려 있었고 **버튼만 없었다.** 내 글에 휴지통 버튼 + 확인창, 하위 댓글도 같이 삭제 |
| 11 | 대숲에 일상 사진 업로드 | ✅ | 글쓰기에 사진 버튼+미리보기. **피드처럼 4:5로 자르지 않는다**(일상 한 장이라 원본 비율이 자연스럽다). `_compressImg`(HEIC 대응) 재사용, imgBB 30초 타임아웃, **업로드를 머피 차감보다 먼저**(실패 시 머피만 빠지는 것 방지), 사진만 올리기 허용, 익명 주의문구, 탭하면 크게 보기. 덤으로 **본문 저장형 XSS 차단**(그대로 innerHTML 이었다) |
| 12 | 홈 센터 고르기 ㄱㄴㄷ 정렬 + 검색 | ✅ | 체크인 탭 `mwPickHome` 은 검색·정렬이 아예 없어 49곳을 통째로 뿌렸다 → 검색칸 신설 + `localeCompare(b,'ko')` 정렬. 온보딩·프로필 피커(`gymPickerRender`)는 검색은 있었고 정렬만 없었다 → 같이 적용. 둘 다 **지역(loc)으로도 검색**된다 |
| 10 | 프로필 '다니는 센터' 1~3개 선택 → 체크인 탭 후보 목록으로 | ✅ | `users.gyms[]`(최대 3) 신설, `gym`엔 첫 번째를 계속 넣어 옛 화면 호환. 프로필은 칩+추가 버튼(#12 피커 재사용, 타깃 `__gyms__`). 체크인 탭에 **'내가 다니는 곳'** 구획 — 레벨·방문수·오늘 완료 여부. 이름으로 매칭(직접 입력은 id가 없어 건너뜀) |
| 8 | 매칭·홈 카드 옆으로 슬라이드 (패스 아님, 되돌아올 수 있어야) | ✅ | `_matchGo(±1)` — **아무것도 소비하지 않고 자리만 이동**. 패스는 패스 버튼일 때만 `_matchPassed`에 담긴다. 손가락 끌기(가로일 때만 가로채 세로 스크롤과 안 싸움) + PC용 좌우 화살표 + `3 / 12` 위치 표시. 끝에서 밀면 살짝 튕김 |
| 13 | 홈 피드 썸네일이 콜드스타트에서 허전 | ✅ | 세 카드가 이미지 없으면 회색 그라디언트로 떨어졌다. **스쿼드**=종목 와이드 필드(`squadFieldSrc`) / **체크인 급상승**=센터 필드(`centerFieldSrc`) / **대숲 인기글**=사진 있는 글 우선(#11 덕에 그림이 생겼다) + 목록이 아니라 **그 글**로 이동 |
| 19 | 머피룸 아이템 미리 입어보기 | ✅ | 대표 확정 = **상점(꾸미기)에서 바로 미리보기**. 예전엔 미보유 아이템을 누르면 **입어보지도 못하고 구매 확인창부터** 떴다(`CREDITS_MODE='enforced'`라 실제로 차감된다). 이제 `charPick`이 시착 시트를 연다 — 큰 캐릭터에 입혀 보여주고 `[되돌리기] / [N머피로 사기]`, 좌우로 같은 슬롯의 다른 미보유 아이템도 계속 입어본다. ★안전장치: 시착 중엔 자동저장(#16)을 안 부르고, 시트를 **모달**로 막았다 — 안 산 옷이 저장되면 영영 공짜로 입고 다니게 된다 |
| 20 | 범프한 사이는 내 프로필 → 머피탭에서도 그 사람 방 보기 | ✅ | 데이터(`users.bumpers`)와 `bumpDogam()`은 이미 있었고 **입구가 스쿼드 화면 하나뿐**이었다(스쿼드에 안 들어가면 도달 불가). → ①프로필 머피 섹션과 ②머피월드 '내 캐릭터' 탭에 **'실제로 만난 머피' 선반** 신설(`mwRenderBumpers`, 캐릭터 가로 스크롤, 탭하면 그 방으로). ③**내 방 방명록에서도** 범프한 사이가 쓴 글은 눌러서 그 사람 방으로 간다(`g.bump && g.from`, 옛 글·내 글은 제외). 방명록은 온라인으로 누구나 쓰지만 범퍼는 만나야 생기므로 '사이'가 있는 쪽만 연다 |

## D. 큰 작업 / 나중

| # | 내용 | 상태 | 메모 |
|---|---|---|---|
| 14 | 그룹 운동 인증 + 회비 + 미달 벌금 | ⬜ | **스쿼드 공금으로 상당 부분 구현돼 있다.** 먼저 점검하고 빈 곳만 채울 것 |
| 18 | 머피캠 필터 — 저장된 사진은 **EXIF 촬영시각**으로 찍히게 | ✅ | `_mwBuildStamp`이 늘 `new Date()`를 찍고 있었다 → `shotAt` 인자 추가. 갤러리 사진은 `openCropModal`에서 **원본 파일의 EXIF**를 미리 읽어 `cropData.shotAt`에 담는다(크롭 blob엔 EXIF가 안 남는다). 외부 라이브러리를 못 써서(CSP) JPEG APP1을 직접 파싱 — `DateTimeOriginal(0x9003)` → 없으면 `DateTime(0x0132)` → 없으면 파일 수정시각(단 1분 이내면 무시) → 그래도 없으면 현재. **II·MM 양쪽 검증 완료**(아이폰은 MM). 바로 찍기는 인자를 안 넘겨 기존대로 현재 시각 |
| 15 | 나이 → **생년월일** (생일 챙기기), 실명·나이 인증 | 🔸 | **생년월일 입력 전환 완료(8-11).** 온보딩·프로필 편집이 `type="date"`로 바뀌고 `users.birth('YYYY-MM-DD')` 저장. 나이는 `_ageFromBirth`로 계산해 `age`에도 같이 넣는다 — age를 읽는 화면이 여럿이라 그쪽을 다 고치는 것보다 값을 맞춰두는 게 안전. **로그인 때 birth로 age를 다시 계산해 어긋나면 고친다**(사람당 1년 1회 쓰기, 안 그러면 나이가 숫자로 굳어 안 는다). 옛 가입자는 birth가 없어 빈칸 + 안내문구, **나이를 역산해 넣지 않는다**(생일을 모르는데 지어내면 안 된다). 만 15~80 검증은 그대로. 경계값 11케이스 테스트 통과. **생일 챙기기도 완료(8-11, 대표 확정)** = ①본인에게만 축하(앱 열면 축하 연출 + `CREDIT_BIRTHDAY=55`머피, `bdayGift=연도`로 1년 1회) ②남에게는 **범프한 사이(일촌)에게만** '오늘 생일' 표시(범퍼 선반·범프 도감). 생일은 개인정보라 아무에게나 안 뿌린다. 남의 문서엔 **`bday:'MM-DD'`만** 복사하고 연도는 안 옮긴다. ★남은 것 = 실명·통신사 인증(네이티브 전환 후) |
| 22 | 히든 캐릭터 여자 버전 3종 | ⬜ | 에셋 제작 |

---

## 처리 순서 (제안)

1. **A 전부** — 지금 들어온 사람이 겪는 것. 특히 2번은 유입이 죽는 문제라 최우선
2. **B** — 대표가 좌표만 주면 끝
3. **C 중 12·4·11·13** — 콜드스타트 체감이 큰 것
4. **C 나머지 → D**

★14번은 새로 만들기 전에 **기존 스쿼드 공금/출석 기능부터 확인**할 것. 이미 있는 걸 또 만들면 안 된다.
