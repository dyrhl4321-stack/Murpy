클로드 작업환경 공통 규칙 초안 (2026-09-09) — 레이지오웬 "플러그인 5개" 적용안
→ 대표가 승인하면 이 내용을 C:\Users\dyrhl\.claude\CLAUDE.md 로 복사 (자동 모드가 그 파일 쓰기를 막아 클로드가 직접 못 씀)
   터미널에서:  ! copy "C:\Users\dyrhl\Murpy\docs\prompts\2026-09-09-claude-env-5tools-rules.txt" "C:\Users\dyrhl\.claude\CLAUDE.md"

────────────────────────────────────────────────────────────────
도구 5개와 발동 시점
────────────────────────────────────────────────────────────────
1. OmniRoute (npm omniroute, 로컬 게이트웨이 http://localhost:20128, GitHub diegosouzapw/OmniRoute)
   - 하는 일: 클로드 한도가 차면 다른 회사 무료 모델(90+ 무료 티어)로 자동 폴백
   - 발동: 한도 메시지가 뜬 뒤에만. 평소엔 끔.
     실행 = C:\Users\dyrhl\.claude\launchers\claude-omni.cmd (OmniRoute 켜고 그 창의 Claude Code 만 우회, 다른 창은 영향 없음)
   - 주의: 코드가 다른 회사 모델로 나간다. Murpy_private/ ·키 파일이 열린 작업엔 쓰지 말 것.
   - 설치(대표가 직접):  ! npm install -g omniroute   → 처음 한 번 대시보드 Keys 에 무료 프로바이더 키 등록

2. Headroom (pip "headroom-ai[all]" 0.37.0, 로컬 프록시 :8787, GitHub chopratejas/headroom)  ✔ 설치됨
   - 하는 일: 파일·로그·툴 출력을 보내기 전에 압축해 토큰 15~95% 절약. 전부 로컬에서 처리.
   - 발동: 로그·큰 파일을 많이 읽는 세션(디버깅·전수 점검)에서 그 창만  headroom wrap claude  로 시작.
     끝나면 반드시  headroom unwrap claude. 상시 켜지 않는다(요약이 원문을 가릴 수 있음).
     런처 = C:\Users\dyrhl\.claude\launchers\claude-headroom.cmd

3. claude-mem (GitHub thedotmack/claude-mem)  — 설치 보류
   - 하는 일: 세션 내용을 AI 로 요약해 다음 세션에 자동 주입
   - 보류 이유: 이 하네스엔 이미 파일 메모리(~/.claude/projects/<id>/memory/MEMORY.md)가 있어 겹치고,
     claude-mem 은 이메일 가입(메모리 키 발급) + 워커 상시 실행이 필요하며 CMEM 이라는 코인 토큰까지 붙어 있음.
   - 원하시면:  ! npx --yes claude-mem@latest install   (설치 중 브라우저 로그인 요구)

4. claude-code-setup (Anthropic 공식 플러그인)
   - 하는 일: 프로젝트를 분석해 맞는 도구·설정을 추천
   - 발동: 새 저장소를 처음 열 때 한 번  /claude-code-setup . 기존 Murpy 는 큰 구조 변경 뒤에만.
   - 설치(대표가 직접):  ! claude plugin install claude-code-setup@claude-plugins-official --scope user

5. Task Observer (GitHub rebelytics/one-skill-to-rule-them-all, CC BY 4.0)  ✔ ~/.claude/skills/task-observer 에 설치됨
   - 하는 일: 작업 중 대표의 정정·반복 작업을 관찰해 스킬 개선점을 기록(관찰 로그 → 스킬 수정 제안)
   - 발동: 세션 시작 프로토콜 + 작업 하나 끝날 때마다 한 줄 보고 (아래 활성화 블록)
   - 워크스페이스: C:\Users\dyrhl\.claude\task-observer (워크트리 밖 고정 경로)

────────────────────────────────────────────────────────────────
기존 워크플로우와의 순서
────────────────────────────────────────────────────────────────
1) 세션 시작 → task-observer 세션 시작 프로토콜 → 메모리 MEMORY.md → 프로젝트 CLAUDE.md
2) 새 기능 = superpowers(brainstorm → plan → 구현). 디자인 = design-taste-frontend / ui-ux-pro-max. 행동 규칙 = andrej-karpathy-skills.
3) 작업 하나가 끝나면 task-observer 관찰 기록 한 줄 보고. 대표가 "그거 왜 고쳐"·"누가 하라고 했음" 류의 정정을 하면
   그 자리에서 관찰 기록 + 메모리(feedback) 둘 다.
4) 한도 메시지 → 지금 창은 두고 새 터미널에서 claude-omni.cmd 로 이어서. 한도 풀리면 평소대로 claude -c.
5) 디버깅·전수 점검처럼 읽는 양이 큰 세션만 headroom wrap claude.

────────────────────────────────────────────────────────────────
Task Observer 활성화 블록 (CLAUDE.md 에 그대로)
────────────────────────────────────────────────────────────────
Before the first tool call of any session — and before writing or proposing a plan, not merely before
executing one — invoke the task-observer skill AND execute its Session Start Protocol (storage check,
frontmatter scan, review trigger). Loading the skill and running the protocol are separate steps; a session
that loads the file and stops has activated nothing. Any turn that will involve a tool call counts; do not
classify the session as "too simple" from its opening message.

After completing each task, check the observation records written this session and report a one-line
summary (ids and titles, or "none logged and why").

Loading a skill is not complete until you have queried the observation log for OPEN observations naming it
and read their bodies:
  grep -l "skill:.*<skill-name>" C:/Users/dyrhl/.claude/task-observer/skill-observations/observation-log/*.md
Apply their insights to the current work, even if the skill file hasn't been updated yet.

The task-observer workspace (user scope) is:  C:/Users/dyrhl/.claude/task-observer
  C:/Users/dyrhl/.claude/task-observer/skill-observations/observation-log/   (the log)
  C:/Users/dyrhl/.claude/task-observer/skill-observations/cross-cutting-principles.md
  C:/Users/dyrhl/.claude/task-observer/skill-updates/                        (staging root)
  C:/Users/dyrhl/.claude/task-observer/skill-updates/PENDING.md              (staging manifest)
Never resolve any of them from the current working directory (a git worktree or temporary clone is torn
down and takes the log with it). This is the single shared location for every project and session on this PC.
