=========================================================================
머피(Murpy) 작업 인수인계 프롬프트 — 2026-08-29 기준 / 배포 v801
이 블록을 통째로 복사해서 코덱스에 붙여넣으세요.
=========================================================================

당신은 Murpy(머피) 프로젝트를 이어서 작업합니다. 아래 규칙을 먼저 읽고,
"다음 작업" 중 지시받은 항목만 손대세요.

-------------------------------------------------------------------------
0. 프로젝트 기본
-------------------------------------------------------------------------
- 저장소: C:\Users\allys\Murpy  (main 브랜치 직접 배포, GitHub Pages)
- 앱 전체가 단일 파일 index.html (약 35,000줄, CSS+HTML+JS 전부)
- 배포 URL: https://murpy.app
- Firebase 프로젝트: murpyprototype (Firestore + Realtime Database + Storage)
- 관리자 계정: dyrhl4321@gmail.com
- 대표(사용자)는 비개발자입니다. 전문용어를 쓰되 바로 쉬운 말로 풀어서 설명하세요.
  항상 한국어 존댓말.

★index.html 의 스크립트 블록 구조 (중요)
  - 14052줄 ~ 35438줄 = <script type="module">  (Firebase import 가 여기 있음)
  - 그 앞 블록들 = 일반 <script>  (module 의 import 를 못 씀)
  - 블록이 다르면 getDoc/updateDoc 같은 걸 직접 못 쓴다. window.* 전역으로 건너간다.

-------------------------------------------------------------------------
1. 고치기 전에 반드시 지킬 것 (어긴 사고가 실제로 있었음)
-------------------------------------------------------------------------
(1) 문법 검사 두 개를 **둘 다** 돌린다. 하나는 module 을 안 본다.
      node tools/dogam-syntax-check.mjs      (일반 script 블록)
      node tools/module-syntax-check.mjs     (module 블록)
    → 2026-08-26 에 else 고아 하나를 놓쳐 module 이 통째로 죽었고
      Firebase 초기화가 안 돼 전 유저 로그인이 막혔다.

(2) 배포 버전을 올린다. 버전이 적힌 곳이 셋이라 스크립트로만 올린다.
      node tools/bump-version.mjs            (sw.js 3곳 + index.html _SW_V + version.txt)
      python tools/check_version.py          (어긋나면 배포 금지)
    → 안 올리면 서비스워커 캐시 때문에 새 코드가 폰에 안 간다.

(3) 겹 순서(캐릭터 레이어)를 손으로 적지 않는다. window._charLayerOrder(cfg) 로만 묻는다.
      node tools/layer-order-check.mjs
    → 사본이 여섯 곳이었고 그중 하나가 overShoes 규칙을 빠뜨려 그 화면만 신발이 바지 위로 올라왔다(3번 반복).

(4) 커밋은 git add 와 commit 을 **한 호출로 묶는다**(다른 창과 섞이는 사고 방지).
    커밋 메시지는 "무엇을 왜"를 한국어로 적는다. 코드 주석도 같은 규칙.

(5) 지울 때는 **지울 것만** 지운다. 객체를 통째로 새로 만들어 저장하지 않는다.
    → 2026-08-29: 방 정리 코드가 `room = { f: 남은가구 }` 로 갈아치워
      **방에 걸어둔 그림(room.art)이 전부 소실**됐다. Object.assign 으로 고쳤고
      roomSaves 슬롯에서 되살리는 복구 코드를 넣었다.

(6) 에셋(옷) 추출은 tools/character-customizer/customizer_cli.py extract-diff 로만 한다.
    새 추출/정리 스크립트를 만들지 않는다. 두 번 만들었다가 두 번 다 옷 본체를 깎아 먹었다.
    지적받은 곳만 고친다("밑단이 번졌다"면 고칠 것은 밑단이다).

(7) 이모지 금지(머피 전용 라인 SVG 아이콘만). 반투명 색면 배지(틴트 칩) 금지.
    픽셀 폰트(Galmuri)는 머피월드 안에서만, 일반 탭은 Pretendard.

(8) 허락 없이 새 UI/새 화면을 만들지 않는다. 기능이 안 되면 "원래 길이 어디였나"부터 찾는다.

(9) 대표가 실기기(아이폰)에서 확인한다. 로컬 미리보기로 "됐어요"라고 하지 않는다.
    막히면 진단 화면: https://murpy.app/?diag=1

-------------------------------------------------------------------------
2. 규칙/배포 명령
-------------------------------------------------------------------------
- Firestore 규칙:   firebase deploy --only firestore:rules --project murpyprototype
    (firebase CLI 가 이 PC 에 설치돼 있고 dyrhl4321@gmail.com 으로 로그인돼 있다.
     firebase 실행 경로: C:\Users\allys\AppData\Roaming\npm\firebase)
- RTDB 규칙:        python tools/deploy_rtdb_rules.py   (브라우저 로그인 1회 필요)
- ★규칙 파일을 커밋하는 것만으로는 배포되지 않는다. 반드시 위 명령까지 돌린다.

-------------------------------------------------------------------------
3. 최근에 끝낸 것 (2026-08-28 ~ 29, v793 ~ v801)
-------------------------------------------------------------------------
v793  같이 그린 그림에 '작품 설명'(60자) 입력 + 장터/상세 표시.
      머피월드에 들어가자마자 펫이 안 뜨던 것(유저 문서가 늦게 와서 목록이 비어 있었다).
v794  8-28 지적 6건:
      · 산 그림 재판매 차단(mwArtCanSell = 가져온 것이면서 그린 사람에 내가 없으면 금지)
      · 채팅 입력줄이 아이폰 키보드 윗줄 바에 가리던 것(_mwPartyKbFix — 가려진 만큼만 올림)
      · 엔터 한 번으로 전송(_mwEnterSend — 한글 조합 중이면 compositionend 뒤에 보냄, 5곳 통일)
      · 낙서판 키보드 가림막(body.mw-jam-kb)
      · 방 명단이 채팅 와야 갱신되던 것 → _mwRlQueueRender 에서 바로 갱신
      · 그림 서명에 12시간 전 사람까지 들어가던 것 → _mwRoomOnline 인 사람만
      · 같이 그린 사람에게도 사본 배달(art_co 알림 → _mwArtCoPick)
      · 꾸미기 저장이 '덮이던' 것(로더가 서버 값으로 덮어씀 → 미뤄 둔 저장이 이김)
v795  운영진(staff)이 스쿼드 공지를 못 고치던 것 = Firestore 규칙이 막고 있었다(배포 완료).
      스쿼드 멤버 목록·필드 캐릭터를 '지금 꾸민 모습'으로(mwCharLive, 3분 캐시).
      옷을 갈아입으면 방·필드에 바로 알림(_mwRlSend/_sqRtSend).
v796  방꾸미기 '핑크 에디션' 1차 36종 (cat:'pink').
v797  망고나시 — 대표가 직접 리터치한 파일 반영(픽셀 손 안 댐).
v798  ★펫 길찾기(_mwPetPath, BFS). 가구에 막히면 와리가리하던 것.
      도착 칸이 막혀 있어도(개집·캣타워·밥그릇은 가구 위다) 목적지로 인정한다.
v799  핑크 2차 — 빠진 것 전부 + 방향 있는 것 dirs 병합(안락의자/의자).
v800  액자 5배로 축소, 7종 제외.
v801  ★방에 걸어둔 그림 소실 사고 수정 + roomSaves 에서 복구.
      핑크 액자 3종 제외(대표 결정). 현재 핑크 에디션 49종.

-------------------------------------------------------------------------
4. 다음 작업 (지시받은 것만 하세요)
-------------------------------------------------------------------------

[A] 손님이 펫을 부를 수 있게 (우선순위 1)
  지금: 펫은 **방 주인만** 부린다. 손님이 "따라와"를 쳐도 펫이 주인을 따라가서
        손님 화면에서는 엉뚱하게 움직인다. 그래서 손님에겐 항상 '자유'로 막아 뒀다.
  해야 할 것: 따라오기의 대상이 **부른 사람**이어야 한다. 채팅 명령(MW_PET_CMDS/
        mwPetChatCmd)과 링 메뉴(mwPetMenu) 양쪽에서 '부른 사람 uid'를 실어 보내고,
        주인 앱이 그 uid 를 향해 펫을 굴린다(펫은 주인이 굴리고 손님은 받아 그린다).
  주의: 실시간 노드는 roomLive/{주인}/pets. RTDB 규칙은 **노드 단위**라 새 하위 키를
        쓰려면 database.rules.json 을 고치고 반드시 배포까지 해야 한다(파일만 고치면 조용히 거부됨).
  검증: 대표 폰(주인) + 다른 계정(손님) 두 대. ?diag=1 에 주인/손님·마지막 올림/거부됨이 나온다.

[B] 여캐 신발·하의 에셋 (우선순위 2)
  · 여캐 흰색/검정 러닝화 = **worn 원본 시트가 없다.** 새로 생성해야 한다.
  · 긴 회색 츄리닝도 새로 뽑아야 한다(지금 등록된 건 반바지다).
  생성 규칙: 나노바나나로 시트를 만들 때 배경은 반드시 형광 그린(#00FF00) 단색.
            투명 배경은 못 그린다. 기장은 수치가 아니라 캐릭터에 이미 있는 선에 못박아야
            프레임마다 안 흔들린다.
  추출: customizer_cli.py extract-diff --slot shoes|bottom (새 스크립트 만들지 말 것)
  검수: python tools/item-purity-check.py <png> --base char/walk_female.png --slot <slot>
        + 12프레임을 어두운 바탕에 깔아 2배로 키운 그림으로 눈으로 확인
        + 대표 승인 전에는 CHAR_ITEMS 에 등록하지 않는다.

[C] 핑크 벽지·바닥 타일 (우선순위 3)
  핑크인테리어.png 왼쪽 위(x0~192, y0~80)에 벽지 3종 + 바닥 타일 4종이 있다.
  이건 가구가 아니라 **방 배경**이라 ROOM_ITEMS 가 아니라 방 배경 시스템에 붙여야 한다.
  현재 방 배경은 char/fields/field_room.png 한 장 고정이다. 갈아끼우는 구조가 없으므로
  설계부터 필요하다(어디서 고르는지·저장 필드·손님 화면 반영). 대표와 먼저 상의할 것.

[D] imgBB 프록시 (출시 전 필수)
  사진 업로드가 무료 imgBB 키 하나에 전 유저가 걸려 있고 키가 공개 저장소에 노출돼 있다.
  8-19 에 imgBB 점검으로 전 유저 인증 업로드가 멈춘 적이 있다.
  Cloudflare Worker 또는 Cloud Functions 프록시로 옮길 것.

[E] 크레딧(머피) 지급을 Cloud Functions 로 이관 (출시 전 필수)
  지금은 클라이언트가 판정하고 자기 문서에 쓴다. 규칙으로 1회 1000 증가 상한만 걸어 뒀다.
  반복하면 뚫린다. 체크인·해금·적립 전부 서버 판정으로 옮겨야 한다.

-------------------------------------------------------------------------
5. 대표 확인 대기 중 (아직 답을 못 받은 것)
-------------------------------------------------------------------------
- 캣타워 흔들림 연출: 좌우 떨림 vs 앞뒤 깜빡임 중 어느 쪽인지
- 핑크 에디션 가격: 지금 소품 5~15 / 큰 가구 20~30 머피. 세트 전체를 공짜로 풀거나
  시즌 한정으로 돌릴지
- v801 의 그림 복구가 실제로 됐는지(앱 열면 "사라졌던 그림 N장을 되살렸어요" 토스트)

-------------------------------------------------------------------------
6. 절대 하지 말 것
-------------------------------------------------------------------------
- char/walk.png (base 시트) 수정
- 비정수배 리샘플 / NEAREST 외 보간
- 아이템이 매칭·피드 노출에 유리해지는 버프
- 해금 조건을 체크리스트로 공개하는 것
- 특허 초안·수익 구조 문서를 공개 저장소에 두는 것 (Murpy_private 에만)
- '벙' 이라는 단어 사용 (벙→스쿼드, 벙주→스쿼드장)
- 모임을 코드가 자동으로 여는 것 (모임은 사람이 직접 연다)

=========================================================================
